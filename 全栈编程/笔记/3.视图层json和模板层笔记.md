## 视图层（json格式的转化规则）

- json格式转化`json.dumps(verb,ensure_ascii=False)` 	其中后面的是为了保证汉字可以正常传输显示

- 像Httprespose()和Jsonrespose()一次只能传输一个变量

- 在非字典类进行传输时，JsonRespose(verb,safe=false) 要进行safe设置

- 还有后端获取图片并且保存到本地

```bash
request总结：

request.POST·获取 

request.POST.get()·获取列表中最后一个元素的数据. 

request.POST.getlist()·获取整个列表 

request.FILES·获取文件数据 

request.GET·获取 

request.path:·获取请求路径, 

request.path_info·:获取请求路径, 

request.get_full_path()·:获取请求路径,可以获取完整的携带数据的路径,
```



## 模板层（自定义标签和模板继承

）

- FCV和CBV模式

- 自定义标签）

第一步(准备工作)

```bash
# 导包
from django import template

# 注册标签博物馆
register = template.Library()


```

第二步（3种方法）

```bash
# 定义过滤器filter()
最多只能传两个参数
# 定义simple_tag()
可以传多个标签
# 定义inclusion_tag()
1.定义的thml内容
2.第一个参数是模板，如show.html
3.返回值只能是以字典的形式进行返回

# 进行标签调用
{% load my_tags %}   定义的.py文件
{% nav %}   # 标签的名字name

```













- 模板继承

  ```bash
  # 主界面
  {% bloak %}
  {% endbloak %}
  # 继承界面
  {% extends 名称 %}
  {% bloak %}
  {% endbloak %}
  
  ```









- 模板导入

  ```bash
  {% include '.html' %}
  ```

  





